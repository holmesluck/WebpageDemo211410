<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Manage</title>
     <!--Bootstrap -->
    <!--<link href="http://cdn.static.runoob.com/libs/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">-->
    <!--&lt;!&ndash; Font Awesome &ndash;&gt;-->
    <!--<link rel="stylesheet" href="http://fontawesome.io/assets/font-awesome/css/font-awesome.css">-->
    <!--<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.8/jquery.min.js"></script>-->
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css"  type="text/css">
    <!-- Font Awesome -->
    <link  rel="stylesheet" href="/stylesheets/font-awesome.css" type="text/css">
    <script src="/js/jquery-3.2.1/jquery-3.2.1.js"></script>
    <script type="text/javascript">
        function checkNull()
        {
            var num=0;
            var str="";
            var str1="";
//            var d =$('#nameuser').val();
//            var e =$('#w3id').val();
//            var reg = /^[A-Za-zd]+([-_.][A-Za-zd]+)*@+(hk1.ibm.com)$/;
//            var reg1 =/ ([a-zA-Z]{2,16}))$/;
            $('.valuenull').each(function(){
                if($(this).val()==="")
                {
//                    alert(d);
//                    alert(e);
//                    if(reg.test()||reg1.test()) {
                    num++;
                    str += "The value of" + " " + $(this).attr('name') + " " + "can not be NULL!!!!\r\n";
//                    }
//                    else
//                    {
//                        alert("It's not a regular username or w3id,please check your input format");
//                    }
                }
            });
            $('.valuenullsp').each(function(){
                if($(this).val()==="")
                {
                    var a = $('.Sa1').val();
                    var b = $('.Sa2').val();
                    var c = $('.Sa3').val();
//                    alert(a);
//                    alert(b);
//                    alert(c);
                    if(a==="Y"||b==="Y"||c==="Y")
                    {
                        num++;
                        str1+="If it contains any selected option of 'Y' in the SaaS selection row"+" "+"The value of"
                            +" "+$(this).attr('name')+" "+"Remarks can not be NULL!!!!\r\n";
                        str+=str1;
                    }
                }
            });
            if(num>0)
            {
                alert(str);
                return false;
            }
//            if(count>0)
//            {
//                alert(str1);
//
//
//            }

            else
            {
                return true;
            }
        }
    </script>
</head>
<body class="nav-md">
